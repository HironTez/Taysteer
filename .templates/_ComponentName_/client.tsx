"use client";

import React from "react";
import { Session } from "next-auth";

interface I_ComponentName_ClientProps {
  session: Session | null;
}

const _ComponentName_Client = (props: I_ComponentName_ClientProps) => {
  return <></>;
};

export default _ComponentName_Client;
